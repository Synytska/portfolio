import { Syne, Roboto_Mono } from 'next/font/google';

export const syne = Syne({ subsets: ['latin'], weight: ['400'] });

export const roboto = Roboto_Mono({ subsets: ['latin'], weight: ['400'], style: 'normal' });

